{
  "name": "@zumer/snapdom-agent",
  "version": "0.0.0",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "description": "Post-layout change oracle for in-page AI agents (working name). NOT FOR PUBLICATION.",
  "type": "module",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./plugin": "./src/plugin.js"
  },
  "peerDependencies": {
    "@zumer/snapdom": "^3.0.0"
  },
  "peerDependenciesMeta": {
    "@zumer/snapdom": {
      "//": "optional so npm does not try to fetch a version that is not published: the host comes from a sibling checkout (tools/host-repo.mjs), never from the registry",
      "optional": true
    }
  },
  "files": [],
  "publishConfig": {
    "access": "restricted"
  },
  "scripts": {
    "test": "node tools/run-bin.mjs vitest run",
    "test:lint": "node tools/run-bin.mjs eslint src test",
    "test:bundles": "node companion/build.mjs && git diff --exit-code -- companion/content.bundle.js",
    "test:regression": "npm run test && npm run test:lint && npm run test:bundles && node experiment/bench-qa.mjs && node experiment/parity.mjs && node experiment/abis-verbs.mjs && npm run test:contract && npm run test:doc-contract && node demo-qa/run-demo.mjs",
    "test:global": "node experiment/abis-verbs.mjs --daemon $HOME/.claude/snapdom-agent/browse.mjs",
    "test:gates": "npm run test:regression && npm run test:adversarial && npm run test:global && node companion/gate.mjs",
    "prepack": "node -e \"throw new Error('@zumer/snapdom-agent is PRIVATE and must never be packed or published. See LICENSE.')\"",
    "prepublishOnly": "node -e \"throw new Error('@zumer/snapdom-agent is PRIVATE and must never be published. See LICENSE.')\"",
    "test:contract": "node experiment/contract-gates.mjs",
    "test:doc-contract": "node experiment/doc-contract.mjs",
    "test:adversarial": "node experiment/adversarial-round.mjs"
  }
}